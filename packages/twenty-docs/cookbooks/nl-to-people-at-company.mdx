---
title: Natural language to people at a company
description: Ask for CHRO / HR leadership / CEO at a company — resolve server-side, search Sales Nav or index.
---

import { TaxonomyQueryFlow } from '/snippets/taxonomy-query-flow.mdx';

Utterances like these should Just Work:

- “Get me the HR leadership at Apple”
- “Give me CHRO-type people at Apple”
- “CEO at StayVista”

You send **role language + company**. Arxena resolves synonyms and seniority **server-side**, maps std function/root/grade to LinkedIn Sales Navigator or Apollo filters when using those sources, then returns people plus a small `resolved` object.

<TaxonomyQueryFlow />

## One call (index)

```bash
curl -X POST \
  -H 'Authorization: Bearer YOUR_API_KEY_TOKEN' \
  -H 'Content-Type: application/json' \
  https://app.arxena.com/people-api/people/search-by-title \
  -d '{
    "jobTitle": "CHRO",
    "companyName": "Apple",
    "dataSource": "index",
    "limit": 10
  }'
```

## Sales Navigator pipeline (Unipile pool)

Classifies the title → maps to Sales Nav `function` IDs + `seniority` → searches via the **shared Unipile Sales Navigator pool** → post-filters by taxonomy.

Require one of:

| Source | Body fields |
| --- | --- |
| Shared Sales Nav pool | `"dataSource": "pool"`, `"candidateSource": "pool"` |
| Harvest Sales Nav | `"dataSource": "harvest"`, `"candidateSource": "harvest"` |
| Explicit Unipile account | `"dataSource": "unipile"`, `"candidateSource": "unipile"`, `"accountId": "..."` |

```bash
curl -X POST \
  -H 'Authorization: Bearer YOUR_API_KEY_TOKEN' \
  -H 'Content-Type: application/json' \
  https://app.arxena.com/people-api/people/search-by-title \
  -d '{
    "jobTitle": "CEO",
    "website": "stayvista.com",
    "dataSource": "pool",
    "candidateSource": "pool",
    "limit": 10
  }'
```

Response includes:

- **`resolved`** — std function / root / grade from the title
- **`query.appliedFilters`** — Sales Nav `functionIds` + `seniorities` (or Apollo department/seniority when `dataSource: "apollo"`)
- **`items`** — people after taxonomy post-filter

## Apollo path

Same endpoint with `"dataSource": "apollo"`. Taxonomy maps to `person_department_or_subdepartments` (including `master_*` roots) and `person_seniorities`.

## What you see back

- **`resolved`** — what the phrase became (function root × grade + confidence)
- **`items`** — filtered people at that company

Playground: [Resolve job title and search people](/api-reference/people/resolve-job-title-and-search-people).

## Related

- [Search people by taxonomy](/cookbooks/search-people-by-taxonomy)
- [Standardized taxonomy](/concepts/standardized-taxonomy)
- [Authentication](/api-reference/authentication)
