import { createState } from 'twenty-ui';

export const dealDiligenceCalendlyEmbedUrlState = createState<string | undefined>(
  {
    key: 'dealDiligenceCalendlyEmbedUrl',
    defaultValue: undefined,
  },
);
