/**
 * Public product slug for the org-chart directory / contact-match channel. Wire
 * value is `m7kq` (older API responses may still use the legacy value; see helper in utils).
 */
export const ORG_CHART_CANDIDATE_SOURCE_M7KQ = 'm7kq' as const;
